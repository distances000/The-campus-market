# 本地回归约定

## 必跑集合

代码改完后，至少执行一次：

```powershell
npm run regress:local
```

这条命令会按顺序执行：

1. 前端生产构建
2. 后端环境自检
3. 数据库结构自检
4. 关键冒烟回归

其中关键冒烟回归包含：

1. 核心业务闭环回归
2. 账号与找回密码回归
3. 最小集成测试
4. 重复提交与并发保护回归

## 上线前全量集合

上线前再执行一次：

```powershell
npm run regress:full
```

它等价于完整 `verify:all`，会在必跑集合之外继续覆盖：

1. 关键页面访问冒烟
2. 并发验证

## 适用范围

以下改动默认必须跑 `regress:local`：

1. 后端路由、权限、状态流转
2. 数据库初始化、迁移、校验
3. 前端页面和接口联动
4. 回归脚本本身

以下改动建议直接跑 `regress:full`：

1. 消息、通知、上传、列表并发相关
2. 页面路由、发布页、消息页、后台页
3. 上线前合并主分支前的最终检查
