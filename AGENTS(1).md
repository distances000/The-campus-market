# AGENTS.md

## 项目背景

这是一个 Web 项目。UI 风格参考 Material Design 3，但不得依赖 Material Web 或其他官方 MD3 组件库。

项目应使用自有组件、design tokens 和 CSS 实现 MD3-like 外观，并保持后续技术栈迁移友好。

## 导入规范

必须遵循项目根目录下的规范文件：

- `./md3-like-web-guidelines.md`

该文件是本项目 Web UI 生成、修改和审查的主要规范。Agent 不需要搜索其他位置，默认从项目根目录读取该文件。

## Agent 执行要求

Agent 在处理 UI 相关代码时必须：

- 优先遵循 `./md3-like-web-guidelines.md`。
- 不引入官方 MD3 组件库。
- 优先使用项目自有组件和 design tokens。
- 避免生成硬编码视觉值或一次性复杂 UI。
- 修改 UI 时同时考虑主题、状态、响应式和基础无障碍。
- 对不符合规范的现有 UI，优先重构为 token 化、组件化实现。

## 语言要求

- 项目界面文案、注释、文档和错误提示统一使用简体中文。
- 不需要预留多语言机制。
- 不引入 i18n、l10n 或语言包相关实现，除非后续明确要求。

## 代码风格要求

- 使用 4 空格缩进。
- 保持代码结构清晰，避免过深嵌套。
- 命名应语义化，避免无意义缩写。
- 优先编写小而清晰的函数和组件。
- 避免重复逻辑，必要时抽取为工具函数、hooks、composable 或组件。
- 避免在业务代码中混入大量样式细节。
- 避免提交无关格式化改动。
- 修改已有代码时，应尽量保持周边代码风格一致。
